{
  "buildCommand": "turbo build --filter=@brainrot/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/web packages *.json *.yaml",
  "github": {
    "silent": true
  }
}