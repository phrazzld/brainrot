{
  "buildCommand": "pnpm build --filter=@brainrot/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": null,
  "devCommand": "pnpm dev --filter=@brainrot/web",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet -- apps/web packages *.json *.yaml",
  "github": {
    "silent": true
  }
}